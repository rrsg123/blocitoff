<div class="row">
	<div class="page-header">
				<h1> <%= @user.email %>'s ToDo List </h1>
        <% if current_user == @user %>
        	<div class="col-md-6">
          		<%= render partial: 'items/form'%>
        	</div>
        <% end %>
	</div>
</div>
<div class="row">
	<%= render partial: 'items/item' %>
</div>
